export const purge = [];
export const darkMode = false;
export const theme = {
  extend: {
    fontFamily: {
      primary: ['Ubuntu', 'sans-serif']
    }
  },
};
export const variants = {
  extend: {},
};
export const plugins = [];
